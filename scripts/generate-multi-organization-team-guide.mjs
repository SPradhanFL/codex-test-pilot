import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';

const scriptDirectory = path.dirname(fileURLToPath(import.meta.url));
const workspace = path.resolve(scriptDirectory, '..');
const outputPath = path.join(workspace, 'reports', 'multi-user-execution-guide', 'index.html');
const organizationIds = ['140462', '140463', '140466'];
const organizations = Object.fromEntries(organizationIds.map((organizationId) => {
  const configPath = path.join(workspace, 'config', `aes-stage.ml.${organizationId}.json`);
  const config = JSON.parse(fs.readFileSync(configPath, 'utf8'));
  return [organizationId, {config, configPath: `config/aes-stage.ml.${organizationId}.json`}];
}));

const controllers = [
  {file:'organization-user-execution.md', name:'Organization User', username:'org_username', password:'org_password', roles:'Organization User', ids:'1–12,14,16–19; 140462 exact login also runs 13A and 13B', numbered:null, contexts:'17 standard; 140462 adds 13A/13B; each listed org adds one Browser Back supplement'},
  {file:'campus-user-execution.md', name:'Campus User', username:'campusUser', password:'campus_password', roles:'Campus User', ids:'3, 7, 14, 16, 17, 20, 21', numbered:7, contexts:'1 Campus User context'},
  {file:'employee-user-execution.md', name:'Employee', username:'employee', password:'employee_password', roles:'Employee', ids:'14, 16', numbered:2, contexts:'1 Employee context'},
  {file:'substitute-user-execution.md', name:'Substitute', username:'substitute', password:'substitute_password', roles:'Substitute', ids:'14, 16', numbered:2, contexts:'1 Substitute context'},
  {file:'multi-role-campus-employee-organization-execution.md', name:'Campus + Employee + Organization User', username:'userRoleSwitcher', password:'roleswitcher_org_password', roles:'Campus → Employee → Organization User', ids:'Campus: 3,7,14,16,17,20,21 · Employee: 14,16 · Organization: 1–12,14,16–19', numbered:26, contexts:'3 role contexts'},
  {file:'multi-role-organization-employee-execution.md', name:'Organization User + Employee', username:'multiRoleOrgEmployee', password:'multi_role_org_employee_password', roles:'Organization User → Employee', ids:'Organization: 1–12,14,16–19 · Employee: 14,16', numbered:19, contexts:'2 role contexts'},
  {file:'multi-role-employee-employee-substitute-execution.md', name:'Employee + Employee + Substitute', username:'multiRoleEmployeeEmployeeSubstitute', password:'multi_role_employee_employee_substitute_password', roles:'Employee 1 → Employee 2 → Substitute', ids:'Each context: 14,16', numbered:6, contexts:'3 role contexts'},
  {file:'multi-org-employee-substitute-execution.md', name:'Multi-org Employee + Substitute', username:'multiOrgEmployeeSubstitute', password:'multi_org_employee_substitute_password', roles:'Every Employee context → every Substitute context', ids:'Each discovered context: 14,16', numbered:null, contexts:'2 per discovered context'},
  {file:'multi-org-employee-employee-execution.md', name:'Multi-org Employee + Employee', username:'multiOrgEmployeeEmployee', password:'multi_org_employee_employee_password', roles:'Every Employee organization context', ids:'Each discovered context: 14,16', numbered:null, contexts:'2 per discovered context'},
  {file:'multi-org-organization-campus-execution.md', name:'Multi-org Organization User + Campus User', username:'multiOrgOrgCampus', password:'multi_org_org_campus_password', roles:'Every Organization User context → every Campus User context', ids:'Organization: 1–12,14,16–19 · Campus: 3,7,14,16,17,20,21', numbered:null, contexts:'17 per Organization context + 7 per Campus context'}
];

const scenarios = [
  [1,'React Home → Extract / Import → legacy Import Data','Organization User','tests/navigation/cross-application-navigation-matrix.md'],
  [2,'Legacy Import Data → Master Data → Employee → General Information','Organization User','tests/employee/general-information/add-employee-validation.md (navigation only)'],
  [3,'React Home → Angular Daily Report','Organization User, Campus User','tests/navigation/cross-application-navigation-matrix.md'],
  [4,'Angular Daily Report → Master Data → Substitute → General Information','Organization User','tests/navigation/angular-daily-report-to-substitute-general-information.md'],
  [5,'Angular Daily Report → Extract / Import → legacy Import Data','Organization User','tests/navigation/angular-daily-report-to-extract_import-to-import-data.md'],
  [6,'Legacy Import Data → Angular Daily Report','Organization User','tests/navigation/cross-application-navigation-matrix.md'],
  [7,'Angular Daily Report → submit global search for report → confirm Search page and navigation elements → React Home','Organization User, Campus User','tests/navigation/cross-application-navigation-matrix.md'],
  [8,'React Home → submit global search for report → confirm Search page and navigation elements → Angular Daily Report','Organization User','tests/navigation/cross-application-navigation-matrix.md'],
  [9,'Legacy Import Data → submit global search for report → confirm Search page and navigation elements → legacy Import Data','Organization User','tests/navigation/cross-application-navigation-matrix.md'],
  [10,'React Home → Role Switcher → confirm every available role is displayed','Organization User','tests/navigation/react-home-page-to-role-switcher-dropdown.md'],
  [11,'Angular Daily Report → Role Switcher → confirm every available role is displayed','Organization User','tests/navigation/angular-daily-report-page-to-role-switcher-dropdown.md'],
  [12,'Legacy Import Data → Role Switcher → confirm every available role is displayed','Organization User','tests/navigation/legacy-import-data-role-switcher.md'],
  [13,'Organization 140462 configured standalone Organization User: Employee General Information → Access → Manage Access; execute migrated-user and non-migrated-user cases independently','Exact configured Organization 140462 standalone Organization User only','tests/navigation/manage-access.md'],
  [14,'Role-adapted absence validation: Organization/Campus/Employee validate absence tabs; Substitute validates four schedule/history views','Organization User, Campus User, Employee, Substitute','tests/navigation/absence-tab.md'],
  [16,'Successfully log out from React Home','Organization User, Campus User, Employee, Substitute','tests/logout/logout-navigation-matrix.md flow 1'],
  [17,'Successfully log out from Angular Daily Report','Organization User, Campus User','tests/logout/logout-navigation-matrix.md flow 2'],
  [18,'Successfully log out from legacy Import Data','Organization User','tests/logout/logout-navigation-matrix.md flow 3'],
  [19,'Successfully log out from Employee maintenance','Organization User','tests/logout/logout-navigation-matrix.md flow 4'],
  [20,'Campus React Home → Reports → Report Writer → confirm page and primary controls','Campus User','instructions/Multi User Instructions/campus-user-execution.md'],
  [21,'Campus React Home → Settings → My Profile → Account Settings → validate and return','Campus User','instructions/Multi User Instructions/campus-user-execution.md']
];

const escapeHtml = (value) => String(value ?? '').replace(/[&<>"']/g, (character) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[character]));
const enabled = (organizationId, controllerFile) => organizations[organizationId].config.enabledControllers.includes(controllerFile);
const organizationControllerCount = (organizationId) => organizations[organizationId].config.enabledControllers.length;
const statusBadge = (organizationId, controllerFile) => enabled(organizationId, controllerFile)
  ? '<span class="badge enabled">Enabled</span>'
  : '<span class="badge disabled">Not enabled</span>';

const fullRunPrompt = (organizationId) => `Start a fresh isolated headed Chrome automation context and newly opened test window. Do not reuse an existing tab, browser history, cookies, or authenticated session.

From the repository root, for OrgId ${organizationId} execute instructions/multi-user-full-suite-execution.md in unattended safe mode. First run scripts/check-multi-user-run-readiness.ps1 -OrgId ${organizationId} and stop only if preflight fails. Read usernames only from config/aes-stage.ml.${organizationId}.json and passwords only from .secrets/aes-stage.ml.${organizationId}.credentials.json. At the first credential-entry point, request one grouped action-time confirmation for all selected accounts and configured Frontline Stage destinations; after confirmation, do not ask again for the same accounts and destinations during this run.

Execute every controller enabled by the selected organization configuration in manifest order, including every documented role/organization context and conditional App Switcher workflow. For App Switcher, validate every displayed enabled application and return to Absence Management; do not require a fixed application inventory. Use headed Chrome through Playwright MCP and continue through independent failures. Before finalizing any FAIL, poll the missing expected page, element, navigation, or state for a full 120 seconds, capture final full-browser evidence at or after timeout, and record failureObservationSeconds: 120 plus the timeout step. For transient overlays or missing page/account shells, use the same 120-second readiness window with one safe refresh after 60 seconds before BLOCKED. Preserve genuine missing-data, permission, safety, and unsupported prerequisites.

Create one continuous 1280x720 full-browser-window video including tabs and the address bar, with no blur, masking, dimming, overlays, annotations, or chapter cards. Validate the configured ${organizations[organizationId].config.requiredUrlContains} URL marker at every workflow checkpoint; report a functional URL mismatch as a supplemental WARNING. Run scripts/start-multi-user-full-suite-run.ps1 -OrgId ${organizationId} so only this organization’s previous current run and ZIP are archived. Generate the canonical master dashboard, one role/login-combination folder per selected controller, detailed scenario pages, full-browser screenshots, measured video timeline, PASS/FAIL/BLOCKED/NOT TESTED/WARNING totals, reproduction steps, and portable ZIP under reports/full-suite/${organizationId}/<timestamp>/. Return the master report path, ZIP path, role-report count, scenario totals, all status totals, and continuous-video duration.`;

const anyOrganizationPrompt = `Start a fresh isolated headed Chrome automation context and newly opened test window. From the repository root, replace <OrgId> with the required configured organization and execute instructions/multi-user-full-suite-execution.md in unattended safe mode. Run scripts/check-multi-user-run-readiness.ps1 -OrgId <OrgId> first and stop only if preflight fails. Read only config/aes-stage.ml.<OrgId>.json and .secrets/aes-stage.ml.<OrgId>.credentials.json. Execute only controllers enabled by that organization, apply dynamic App Switcher validation, continue through independent failures, and wait/poll a full 120 seconds before finalizing any FAIL. For transient overlays or missing page/account shells, use the same 120-second readiness window with one safe refresh after 60 seconds before BLOCKED. Record one continuous 1280x720 headed-Chrome outer-window video including the address bar, with no blur or overlays. Run scripts/start-multi-user-full-suite-run.ps1 -OrgId <OrgId>, archive only that organization’s prior current run and ZIP, and generate the canonical report plus portable ZIP under reports/full-suite/<OrgId>/<timestamp>/.`;

const singleControllerPrompt = `Start a fresh isolated headed Chrome automation context and newly opened test window. From the repository root, for OrgId <OrgId> execute instructions/Multi User Instructions/<controller-file> in unattended safe mode. First run scripts/check-multi-user-run-readiness.ps1 -OrgId <OrgId> -Controller <controller-file> and stop only if preflight fails. Use only the matching organization-scoped configuration and credentials, and reject a controller that is not in enabledControllers. Execute every documented role/organization context, apply dynamic App Switcher validation wherever visible, preserve the role-specific Scenario 14 behavior, and continue through independent failures. Before finalizing FAIL, poll for 120 seconds and capture final evidence. For transient overlays or missing page/account shells, use the same 120-second readiness window with one safe refresh after 60 seconds before BLOCKED. Record one continuous 1280x720 full-browser video with address bar visible and no blur or overlays. Start, finalize, generate, and package the canonical organization-scoped evidence under reports/full-suite/<OrgId>/<timestamp>/.`;

const controllerRows = controllers.map((controller, index) => `<tr data-controller="${escapeHtml(controller.name.toLowerCase())}" ${organizationIds.map((organizationId) => `data-org${organizationId}="${enabled(organizationId, controller.file)}"`).join(' ')}><td>${index + 1}</td><td><strong>${escapeHtml(controller.name)}</strong><br><code>${escapeHtml(controller.file)}</code><br><span class="muted">${escapeHtml(controller.roles)}</span></td><td><code>${escapeHtml(controller.username)}</code><br><code>${escapeHtml(controller.password)}</code></td><td>${escapeHtml(controller.ids)}</td><td>${escapeHtml(controller.contexts)}</td>${organizationIds.map((organizationId) => `<td>${statusBadge(organizationId, controller.file)}</td>`).join('')}</tr>`).join('');
const scenarioRows = scenarios.map(([id, scenario, roles, source]) => `<tr><td><span class="scenario-id">${id}</span></td><td>${escapeHtml(scenario)}</td><td>${escapeHtml(roles)}</td><td><code>${escapeHtml(source)}</code></td></tr>`).join('');

const organizationCards = organizationIds.map((organizationId) => {
  const {config, configPath} = organizations[organizationId];
  const manageAccess = config.scenarioData?.manageAccess;
  const gate = manageAccess?.enabled
    ? `Scenario 13 is enabled only for the exact standalone <code>${escapeHtml(manageAccess.loginUsernameKey)}</code> mapping and produces ${manageAccess.cases?.length ?? 0} independent case outcomes.`
    : 'Scenario 13 is outside this organization’s scope and is omitted entirely; it is not reported as NOT TESTED or BLOCKED.';
  return `<article class="org-card"><div class="card-top"><div><span class="eyebrow dark">Organization</span><h3>${organizationId}</h3></div><span class="badge org">${organizationControllerCount(organizationId)} controllers</span></div><p><strong>Configuration:</strong> <code>${escapeHtml(configPath)}</code></p><p><strong>Local credentials:</strong> <code>.secrets/aes-stage.ml.${organizationId}.credentials.json</code></p><p><strong>Report root:</strong> <code>reports/full-suite/${organizationId}/</code></p><p><strong>Scope:</strong> Dynamic from enabled controllers and discovered role/organization contexts.</p><p>${gate}</p><button class="btn" onclick="copyById('prompt-${organizationId}',this)">Copy ${organizationId} full-run prompt</button></article>`;
}).join('');

const generatedAt = new Intl.DateTimeFormat('en-IN', {dateStyle:'medium', timeStyle:'short', timeZone:'Asia/Kolkata'}).format(new Date());
const css = `
:root{--navy:#102b4e;--blue:#2567c9;--bg:#eef3f8;--panel:#fff;--ink:#172033;--muted:#5e6c80;--line:#d8e1ec;--pass:#16835a;--warn:#a96808;--danger:#b83232;--info:#7752aa;--shadow:0 10px 28px rgba(30,48,75,.09)}
*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;color:var(--ink);background:linear-gradient(180deg,#e8f0fa 0,#f7f9fc 380px);font:15px/1.55 Inter,Segoe UI,Arial,sans-serif}.wrap{width:min(1260px,calc(100% - 32px));margin:auto}.hero{padding:40px 0 36px;color:#fff;background:linear-gradient(135deg,#0d2749,#245f9e)}.eyebrow{font-size:12px;font-weight:800;letter-spacing:.12em;text-transform:uppercase;opacity:.82}.eyebrow.dark{color:var(--muted)}.hero h1{margin:7px 0 9px;font-size:36px;line-height:1.14}.hero p{max-width:1000px;margin:0;color:#dfebfa}.chips,.links,.filter-bar{display:flex;flex-wrap:wrap;gap:8px}.chips{margin-top:18px}.chip{padding:6px 10px;border:1px solid rgba(255,255,255,.22);border-radius:999px;background:rgba(255,255,255,.12);font-size:13px}.links{margin-top:15px}.hero-link{color:#fff;background:rgba(255,255,255,.13);border:1px solid rgba(255,255,255,.2);border-radius:999px;padding:7px 11px;text-decoration:none;font-weight:700}.main{padding:26px 0 52px}.grid{display:grid;gap:15px}.stats{grid-template-columns:repeat(5,1fr);margin-bottom:18px}.stat,.panel,.org-card,.role-card{border:1px solid var(--line);border-radius:14px;background:var(--panel);box-shadow:var(--shadow)}.stat{padding:18px}.stat b{display:block;font-size:29px}.stat span,.muted{color:var(--muted)}.panel{padding:20px;margin-bottom:18px;scroll-margin-top:82px}.panel h2{margin:0 0 12px;font-size:22px}.panel h3{margin:16px 0 7px}.notice{border-left:5px solid var(--warn);background:#fffaf0}.success{border-left:5px solid var(--pass);background:#f5fcf9}.info{border-left:5px solid var(--info);background:#faf8ff}.toc{display:flex;gap:8px;flex-wrap:wrap;padding:14px;margin-bottom:18px;position:sticky;top:8px;z-index:5;border:1px solid var(--line);border-radius:14px;background:rgba(255,255,255,.96);box-shadow:var(--shadow)}.toc a{color:var(--navy);background:#e7eef9;border-radius:999px;padding:6px 10px;text-decoration:none;font-size:13px;font-weight:700}.org-grid{grid-template-columns:repeat(2,1fr)}.org-card{padding:19px;border-top:5px solid var(--blue)}.org-card h3{font-size:28px;margin:2px 0}.org-card p{margin:8px 0}.card-top{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.role-grid{grid-template-columns:repeat(4,1fr)}.role-card{padding:17px;border-top:4px solid var(--blue)}.role-card h3{margin:0 0 5px}.role-card b{font-size:22px}.table-wrap{overflow:auto;border:1px solid var(--line);border-radius:10px}table{width:100%;border-collapse:collapse;background:#fff}th,td{padding:11px 10px;border-bottom:1px solid var(--line);vertical-align:top;text-align:left}th{position:sticky;top:0;background:#f2f6fb;color:var(--muted);font-size:12px;letter-spacing:.06em;text-transform:uppercase;white-space:nowrap}tr:last-child td{border-bottom:0}code{font:12px Consolas,monospace;color:#40506a;word-break:break-word}.badge{display:inline-block;padding:4px 9px;border-radius:999px;color:#fff;font-size:12px;font-weight:800;white-space:nowrap}.enabled{background:var(--pass)}.disabled{background:#8792a3}.org{background:var(--blue)}.warning{background:var(--warn)}.scenario-id{display:inline-grid;place-items:center;width:29px;height:29px;border-radius:50%;background:#e5eefb;color:var(--navy);font-weight:800}.filter-bar{margin:12px 0}.filter-bar input{min-width:260px;flex:1;padding:10px 12px;border:1px solid #bfcadd;border-radius:9px;font:inherit}.btn,.filter-btn,.copy{border:0;border-radius:9px;background:var(--blue);color:#fff;padding:9px 13px;font-weight:750;cursor:pointer}.filter-btn{background:#e7eef9;color:var(--navy)}.filter-btn.active{background:var(--navy);color:#fff}.copy.done,.btn.done{background:var(--pass)}.code-card{border:1px solid #253755;border-radius:11px;overflow:hidden;margin:14px 0;background:#111827}.code-head{display:flex;justify-content:space-between;align-items:center;background:#1d2b42;color:#dce9fb;padding:9px 12px;font-size:12px;font-weight:700;text-transform:uppercase;letter-spacing:.06em}pre{margin:0;padding:17px;white-space:pre-wrap;word-break:break-word;overflow:auto;color:#e5e7eb;font:13px/1.55 Consolas,monospace}.steps{padding-left:23px}.steps li+li{margin-top:6px}.tree{background:#111827;color:#e5e7eb;padding:16px;border-radius:10px;font:13px/1.55 Consolas,monospace;white-space:pre-wrap}.footer{padding:0 0 28px;color:var(--muted);text-align:center;font-size:13px}@media(max-width:980px){.stats{grid-template-columns:repeat(2,1fr)}.role-grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:720px){.stats,.role-grid,.org-grid{grid-template-columns:1fr}.hero h1{font-size:29px}.toc{position:static}}@media print{body{background:#fff}.hero{background:#fff;color:#000;padding:10px 0 20px}.hero p{color:#444}.chip,.hero-link{border-color:#aaa;background:#fff;color:#000}.toc,.copy,.btn,.filter-bar{display:none}.panel,.stat,.org-card,.role-card{box-shadow:none;break-inside:avoid}.wrap{width:100%}th{position:static}}
`;

const html = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Multi-Organization Controller Coverage & Execution Guide</title><style>${css}</style></head>
<body><header class="hero"><div class="wrap"><div class="eyebrow">AES Stage · Multi-organization team handoff</div><h1>Controller Coverage & Execution Guide</h1><p>Current organization-scoped coverage, secure credential routing, corrected scenario behavior, copy-ready prompts, and canonical evidence requirements for organizations 140462, 140463, and 140466.</p><div class="chips"><span class="chip">Generated ${escapeHtml(generatedAt)}</span><span class="chip">20 active scenario IDs</span><span class="chip">10 controller types</span><span class="chip">3 organization configurations</span><span class="chip">120-second failure policy</span><span class="chip">No credentials embedded</span></div><div class="links"><a class="hero-link" href="#prompts">Copy execution prompts</a><a class="hero-link" href="#controller-matrix">Controller matrix</a><a class="hero-link" href="#scenario-catalog">Scenario catalog</a></div></div></header>
<main class="main"><div class="wrap">
<section class="grid stats"><div class="stat"><b>20</b><span>Active scenario IDs</span></div><div class="stat"><b>4</b><span>Application roles</span></div><div class="stat"><b>10</b><span>Controller types</span></div>${organizationIds.map((organizationId) => `<div class="stat"><b>${organizationControllerCount(organizationId)}</b><span>Enabled for ${organizationId}</span></div>`).join('')}</section>
<nav class="toc"><a href="#changes">What changed</a><a href="#organizations">Organizations</a><a href="#roles">Role coverage</a><a href="#controller-matrix">Controllers</a><a href="#scenario-catalog">20 active scenarios</a><a href="#prompts">Prompts</a><a href="#reporting">Evidence & reports</a><a href="#setup">Secure setup</a></nav>

<section class="panel notice" id="changes"><h2>What changed from the earlier reference files</h2><ul class="steps"><li>Configuration and local credentials are isolated by organization ID: <code>aes-stage.ml.&lt;OrgId&gt;.json</code> and <code>aes-stage.ml.&lt;OrgId&gt;.credentials.json</code>.</li><li>Numbered logout scenarios stop after the approved login page displays; they do not click browser Back or test direct protected routes. One separate standalone Organization User Browser Back workflow runs for each of 140462, 140463, and 140466—three validations total.</li><li>Scenario 15, Security → Manage User Access, is retired. Scenario 13 runs only for the exact standalone Organization 140462 mapping and produces two independent Manage Access outcomes; all other contexts omit it.</li><li>Campus User has seven scenarios: <strong>3, 7, 14, 16, 17, 20, and 21</strong>.</li><li>Substitute Scenario 14 validates <strong>Available Jobs, Scheduled Jobs/Schedule, Past Jobs/History Jobs, and Non Work Days</strong>. An individual job is not required, and a valid zero-job state is acceptable.</li><li>Every potential FAIL receives a full <strong>120-second observation/polling window</strong>. A future report is rejected when a FAIL does not record <code>failureObservationSeconds: 120</code> and its timeout step. Transient overlays and missing page/account shells receive the same 120-second readiness window with one safe refresh after 60 seconds.</li><li>App Switcher validation is dynamic: test every displayed enabled application and return to Absence Management. Do not require a fixed inventory.</li><li>Every invocation uses a fresh isolated headed Chrome window and one continuous full-browser video including the address bar, with no blur, masking, dimming, overlays, annotations, or chapter cards.</li><li>Reports and archives are isolated under <code>reports/full-suite/&lt;OrgId&gt;/</code>; starting a run archives only that organization’s previous current report and ZIP.</li><li>The stable URL marker comes from the selected organization config. A mismatch is a separate WARNING when functional navigation still works.</li></ul></section>

<section id="organizations"><h2>Organization routing</h2><div class="grid org-grid">${organizationCards}</div></section>

<section class="panel" id="roles"><h2>Role authorization summary</h2><div class="grid role-grid"><article class="role-card"><h3>Organization User</h3><b>17 standard</b><p>IDs <code>1–12, 14, 16–19</code>; the exact 140462 gate adds Scenario 13A and 13B. Every standalone Organization User also runs one Browser Back-after-logout supplement.</p></article><article class="role-card"><h3>Campus User</h3><b>7 scenarios</b><p>IDs <code>3, 7, 14, 16, 17, 20, 21</code></p></article><article class="role-card"><h3>Employee</h3><b>2 scenarios</b><p>IDs <code>14, 16</code></p></article><article class="role-card"><h3>Substitute</h3><b>2 scenarios</b><p>IDs <code>14, 16</code>; Scenario 14 uses the four-view Substitute flow.</p></article></div><p class="muted">Retired Scenario 15 never runs. Browser Back after logout is tested only once per organization through the standalone Organization User controller. A visible App Switcher creates one supplemental workflow per distinct role/organization context. Supplemental workflows are not numbered, so final workflow totals may exceed the numbered scenario totals.</p></section>

<section class="panel" id="controller-matrix"><h2>Controller, credential-key, and organization mapping</h2><p class="muted">Only key names are shown. Username values remain in the selected organization config; password values remain in the ignored organization credentials file.</p><div class="filter-bar"><input id="controllerSearch" type="search" placeholder="Search controller, role, key, or scenario IDs" oninput="filterControllers()"><button class="filter-btn active" data-filter="all" onclick="setControllerFilter('all',this)">All</button>${organizationIds.map((organizationId) => `<button class="filter-btn" data-filter="${organizationId}" onclick="setControllerFilter('${organizationId}',this)">${organizationId} enabled</button>`).join('')}</div><div class="table-wrap"><table><thead><tr><th>#</th><th>Controller / role order</th><th>Credential keys</th><th>Scenario coverage</th><th>Context outcomes</th>${organizationIds.map((organizationId) => `<th>${organizationId}</th>`).join('')}</tr></thead><tbody id="controllerRows">${controllerRows}</tbody></table></div></section>

<section class="panel info"><h2>Role-adapted Scenario 14</h2><ul class="steps"><li><strong>Organization User:</strong> Prefer an existing absence; otherwise use only a uniquely verified safe create → validate → cleanup lifecycle.</li><li><strong>Campus User:</strong> Prefer an existing absence; create only when both safe creation and cleanup are exposed in the same Campus context.</li><li><strong>Employee:</strong> Prefer an existing self-service absence; create only when the current identity can safely reopen and remove the exact record without substitute notification.</li><li><strong>Substitute:</strong> Never require or create a job. Click and validate Available Jobs, Scheduled Jobs/Schedule, Past Jobs/History Jobs, and Non Work Days. Empty states pass when the controls and content are responsive.</li></ul></section>

<section class="panel" id="scenario-catalog"><h2>Complete active 20-scenario catalog</h2><div class="filter-bar"><input id="scenarioSearch" type="search" placeholder="Search by ID, scenario, role, or source" oninput="filterScenarios()"><button class="btn" onclick="window.print()">Print / Save as PDF</button></div><div class="table-wrap"><table><thead><tr><th>ID</th><th>Scenario</th><th>Authorized roles</th><th>Source</th></tr></thead><tbody id="scenarioRows">${scenarioRows}</tbody></table></div></section>

<section class="panel success" id="reporting"><h2>Canonical execution and reporting requirements</h2><ol class="steps"><li>Run readiness with the selected <code>OrgId</code>; execute only its <code>enabledControllers</code> in manifest order.</li><li>Use one fresh isolated headed Chrome context and one dedicated 1280×720 browser window for the complete selected run.</li><li>Record one continuous Chrome outer-window video, including tabs and the address bar. Capture measured account/workflow boundaries; never estimate or evenly divide video ranges.</li><li>Capture a full-browser screenshot for every workflow and additional final evidence for FAIL, BLOCKED, NOT TESTED, and WARNING outcomes.</li><li>Wait/poll 120 seconds before FAIL. For transient overlays or missing page/account shells, use the same 120-second readiness window with one safe refresh after 60 seconds; do not delay or reclassify genuine missing-data, permission, safety, or intentional data-gate outcomes.</li><li>Continue through independent failures. Preserve role/context identity and repeat shared scenario IDs for every applicable context.</li><li>Generate the master <code>index.html</code>, one self-contained role/login-combination folder, scenario pages, screenshots, measured timeline, one shared video, totals, reproduction steps, and portable ZIP.</li></ol><div class="tree">reports/full-suite/&lt;OrgId&gt;/&lt;runId&gt;/
├── index.html
├── run-data.json
├── video-events.json
├── timeline.json
├── report-format.json
├── roles/&lt;login-combination&gt;/
│   ├── index.html
│   ├── scenarios/&lt;scenario&gt;.html
│   └── screenshots/&lt;evidence&gt;.png
└── videos/multi-user-full-suite-execution.webm

Portable ZIP: reports/full-suite/&lt;OrgId&gt;/multi-user-full-suite-&lt;OrgId&gt;-&lt;runId&gt;.zip
Archives: reports/full-suite/&lt;OrgId&gt;/old-reports/old-&lt;previous-runId&gt;/</div></section>

<section id="prompts"><h2>Copy-ready execution prompts</h2>${organizationIds.map((organizationId) => `<section class="panel"><h3>Organization ${organizationId} — all ${organizationControllerCount(organizationId)} enabled controllers</h3><div class="code-card"><div class="code-head"><span>Full organization run</span><button class="copy" onclick="copyById('prompt-${organizationId}',this)">Copy</button></div><pre id="prompt-${organizationId}">${escapeHtml(fullRunPrompt(organizationId))}</pre></div></section>`).join('')}<section class="panel"><h3>Any configured organization</h3><div class="code-card"><div class="code-head"><span>Reusable full-run prompt</span><button class="copy" onclick="copyById('prompt-any',this)">Copy</button></div><pre id="prompt-any">${escapeHtml(anyOrganizationPrompt)}</pre></div></section><section class="panel"><h3>One controller for one organization</h3><div class="code-card"><div class="code-head"><span>Reusable controller prompt</span><button class="copy" onclick="copyById('prompt-one',this)">Copy</button></div><pre id="prompt-one">${escapeHtml(singleControllerPrompt)}</pre></div></section></section>

<section class="panel" id="setup"><h2>Secure setup after clone or pull</h2><ol class="steps"><li>Choose the organization ID.</li><li>Confirm <code>config/aes-stage.ml.&lt;OrgId&gt;.json</code> exists and contains the intended enabled controllers.</li><li>Create the ignored local credential file:</li></ol><div class="code-card"><div class="code-head"><span>PowerShell</span><button class="copy" onclick="copyById('setup-copy',this)">Copy</button></div><pre id="setup-copy">Copy-Item .secrets/aes-stage.ml.&lt;OrgId&gt;.credentials.example.json .secrets/aes-stage.ml.&lt;OrgId&gt;.credentials.json</pre></div><ol class="steps" start="4"><li>Replace every required placeholder locally. Never commit the real credentials file.</li><li>Run readiness:</li></ol><div class="code-card"><div class="code-head"><span>PowerShell</span><button class="copy" onclick="copyById('setup-ready',this)">Copy</button></div><pre id="setup-ready">powershell -ExecutionPolicy Bypass -File scripts/check-multi-user-run-readiness.ps1 -OrgId &lt;OrgId&gt;</pre></div><p class="muted">Never put passwords, tokens, cookies, authorization data, sensitive redirect fragments, or personal test data in prompts, reports, screenshots, video filenames, or committed files.</p></section>
</div></main><footer class="footer"><div class="wrap">Generated from current repository configuration and controller rules. No usernames, passwords, or session data are embedded.</div></footer>
<script>
let controllerFilter='all';
function filterControllers(){const query=document.getElementById('controllerSearch').value.toLowerCase();for(const row of document.querySelectorAll('#controllerRows tr')){const matchesText=row.innerText.toLowerCase().includes(query);const matchesOrg=controllerFilter==='all'||row.dataset['org'+controllerFilter]==='true';row.hidden=!(matchesText&&matchesOrg)}}
function setControllerFilter(value,button){controllerFilter=value;document.querySelectorAll('.filter-btn').forEach((item)=>item.classList.remove('active'));button.classList.add('active');filterControllers()}
function filterScenarios(){const query=document.getElementById('scenarioSearch').value.toLowerCase();for(const row of document.querySelectorAll('#scenarioRows tr'))row.hidden=!row.innerText.toLowerCase().includes(query)}
async function copyById(id,button){const text=document.getElementById(id).innerText;let copied=false;try{await navigator.clipboard.writeText(text);copied=true}catch(error){const area=document.createElement('textarea');area.value=text;area.style.position='fixed';area.style.opacity='0';document.body.appendChild(area);area.select();copied=document.execCommand('copy');area.remove()}if(copied){const original=button.textContent;button.textContent='Copied';button.classList.add('done');setTimeout(()=>{button.textContent=original;button.classList.remove('done')},1500)}}
</script></body></html>`;

fs.mkdirSync(path.dirname(outputPath), {recursive:true});
fs.writeFileSync(outputPath, html, 'utf8');
console.log(`Generated ${path.relative(workspace, outputPath)}`);
