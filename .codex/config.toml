# Project-scoped Playwright MCP server for live browser execution.
[mcp_servers.playwright]
command = "npx"
args = ["-y", "@playwright/mcp@latest", "--browser", "chrome", "--codegen", "none"]
enabled = true
startup_timeout_sec = 30
tool_timeout_sec = 120
